build:
  box: docker
  docker: true
  steps:
    - script:
        name: Build image
        code: |
          docker build -t ateam-mp:latest .
    - script:
        name: Run docker ps to see the image we just created
        code: |
          docker ps

